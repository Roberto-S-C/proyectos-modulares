import { StyleSheet } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function EvaluatorProjectsScreen() {
    return(
        <SafeAreaView style={styles.screen}>
            
        </SafeAreaView>
    );
}

const styles = StyleSheet.create({
    screen: {
        flex: 1,
    }
});